import './styles.scss';

const initialHtmlDump = require('./initial-html-dump');
const addRandomCharacter = require('./add-random-character');
const removeCharacter = require('./remove-character');
const spinAndChangeDirection = require('./spin-and-change-direction');

initialHtmlDump.init();
addRandomCharacter.init();
removeCharacter.init();
spinAndChangeDirection.init();